apiVersion: v1
kind: Secret
metadata:
  name: stock-monitor-secret
  namespace: stock-monitor
  labels:
    app: stock-monitor
type: Opaque
data:
  # Base64-encoded Finnhub API key
  # Generate with: echo -n 'YOUR_API_KEY' | base64
  REACT_APP_FINNHUB_API_KEY: REPLACE_WITH_BASE64_ENCODED_API_KEY
